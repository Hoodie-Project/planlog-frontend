# Progress

## 101. 포토 완료 스탬프 에셋 교체 (2026-09-25)

- 완료한 스탬프의 포토 카드 아이콘을 `completed-stamp-photo.svg` 에셋으로 교체했다.

## 100. 문화 완료 스탬프 에셋 교체 (2026-09-24)

- 완료한 스탬프의 문화 카드 아이콘을 `completed-stamp-coffee.svg` 에셋으로 교체했다.

## 99. 스탬프 리뷰 모달 닫기 복원 (2026-09-24)

- 스탬프 리뷰 모달을 X 버튼 및 배경 클릭으로 닫을 수 있게 복원했다. 닫아도 스탬프는 생성되지 않으며, 리뷰를 저장할 때만 생성된다.

## 98. 추천 코스 시작·종료 상태 전환 안정화 (2026-09-24)

- 코스 시작 API 완료 후에만 활성 저장 코스 ID를 갱신해, 지연된 `PENDING` 조회 응답이 `IN_PROGRESS` 상태를 덮어쓰지 않도록 수정했다.
- 시작 완료 시 버튼은 코스 종료하기로 전환되고, 종료 버튼은 코스 리뷰 모달을 연 뒤 저장 완료 시 코스를 완료 상태로 변경한다.

## 97. 코스 후기 전체 장소 및 선택 필터 반영 (2026-09-24)

- 코스 후기 API에 `SPOT` 타입만이 아닌 추천 코스의 모든 장소 `contentId`를 전달하도록 수정했다.
- 지도에 코스 전체 장소를 표시하고, 마커 선택 시 해당 장소의 후기만 목록에서 보여 주며 전체 보기로 필터를 해제할 수 있게 했다.

## 96. 리뷰 감정 API enum 정합성 보완 (2026-09-24)

- 리뷰 모달의 화면 표기 “감사하는”을 유지하면서, 기록 API에는 백엔드 enum 값인 “감사한”을 전송하도록 분리했다.
- 기존 기록의 “감사하는” 값도 읽기 모달에서 정상적으로 선택 상태로 표시되게 정규화했다.

## 95. 리뷰 완료 기반 스탬프 획득 흐름 적용 (2026-09-24)

- 추천 코스의 스탬프 받기 버튼은 더 이상 즉시 스탬프를 생성하지 않고, 리뷰 입력 모달을 먼저 연다.
- 리뷰 저장 시에만 스탬프 생성과 장소 기록 저장을 수행하며, 이 획득용 모달은 배경 클릭 및 닫기 버튼으로 닫을 수 없게 했다.

## 94. 스탬프 한 줄 평가 입력 제한 확대 (2026-09-24)

- 스탬프 리뷰 모달의 한 줄 평가 입력 제한을 15자에서 50자로 확대하고 안내 문구를 맞췄다.

## 93. 랜딩 축제 카드 그림자 여백 보완 (2026-09-24)

- 축제 캐러셀의 스크롤 영역에 상하좌우 여백을 추가해 카드 외곽 그림자가 `overflow`에 의해 잘리지 않도록 수정했다.

## 92. 랜딩 축제 캐러셀 가림막 제거 (2026-09-24)

- 축제 카드 양끝을 덮던 데스크톱 그라데이션 오버레이를 제거해 카드 전체가 선명하게 보이도록 수정했다.

## 91. 랜딩 페이지 데스크톱 카드 잘림 보완 (2026-09-24)

- 축제 캐러셀의 3열 카드 폭 계산식을 유효한 CSS `calc()` 형식으로 정정해 카드 3개와 간격이 컨테이너 안에 정확히 맞도록 수정했다.
- 코스 만드는 방법은 `xl` 이상에서만 5개 카드를 한 줄로 배치하고 카드 폭을 216px로 조정해, 1240px 컨테이너의 우측 카드가 잘리지 않도록 했다.
- 태블릿 이하에서는 가로 스크롤로 각 카드를 온전히 확인할 수 있다.

## 현재 상태

- 2026-07-12 기준 초기 프로젝트 구조 생성 완료
- 문서 3종 작성 완료
- Next.js + TypeScript + Tailwind 기반 프론트엔드 스캐폴딩 생성 완료
- 주요 화면용 목업 데이터와 라우트 초안 생성 완료
- 코스 만들기 4단계 쿼리스트링 뼈대와 API fetch 레이어 생성 완료
- GitHub Actions self-hosted runner 기반 배포 파이프라인 초안 추가 예정

## 이번 턴에서 한 작업

1. `src/api/courses/generate.ts` 코스 생성 fetch 함수 추가
2. `src/store/course-store.ts`를 `zustand persist + sessionStorage` 기반 다단계 초안 저장 구조로 확장
3. `/course/create?step=1..4` 쿼리스트링 기반 무드 셀렉터 뼈대 구현
4. API 응답 타입과 결과 페이지 연결용 뷰 모델 추가
5. 아키텍처/진행 문서에 상태 관리와 API 경계를 반영
6. 로그인 모달 Figma 구현 및 보호 라우트 진입 가드 추가
7. `zustand persist + localStorage` 기반 mock auth 스토어/유저 타입 추가
8. `deploy.sh`를 GitHub Actions checkout 기준으로 정리
9. `.github/workflows/deploy.yml` 배포 workflow 추가
10. README에 self-hosted runner 서비스 전환 절차 추가
11. workflow 배포 기준 경로를 `/Users/macmini/planlog/planlog-frontend`로 고정
12. 피그마 `1286:1809` 기준 랜딩페이지 레이아웃/카드/스탬프 섹션 재정렬
13. `추천 코스`, `나의 기록` 페이지를 기존 디자인 시스템 기준 Mock UI로 확장
14. `추천 코스` 결과 페이지를 최신 화면 기획안 기준으로 재구성하고 숙소 선택/직접 입력 Mock 모달 추가
15. `코스 상세` 페이지를 추천 코스 연계 상세 화면으로 재구성하고 브레드크럼브/일정표/지도 Mock 추가
16. 추천 코스/코스 상세/나의 기록 Mock UI 공통 패턴을 shared mock 컴포넌트로 정리
17. 카카오 로그인, 게스트 로그인, `/api/auth/me` 기반 실제 인증 흐름으로 mock auth 제거
18. Next `/api/auth/*` proxy route 추가로 로컬 개발 환경 CORS 이슈 대응
19. `inyong`과 `dev` 비교 후, 현재 브랜치의 실제 백엔드 연동(코스 생성·저장 코스·스탬프·북마크)을 유지
20. 모든 브라우저 API 호출을 공통 `apiFetch`로 통합해 인증 헤더, query string, JSON/비JSON 오류 처리를 일관화
21. 저장 코스·북마크의 동적 식별자를 프록시 전달 전에 URL 인코딩하고 프로덕션 빌드 검증 완료
22. 업스트림 API의 `/api` 접두사 누락으로 발생한 코스 생성 404를 수정하고, 공통 프록시에서 모든 비인증 API 경로의 접두사를 보장
23. 인증 프록시의 업스트림 경로를 Swagger 명세(`/api/auth/*`)와 일치시켜 게스트·카카오·내 정보 요청의 404 수정
24. 추천 코스 지도 화면·추천 숙소 화면·완료 스탬프와 기록 화면을 병합하고, 추천 코스 저장은 실제 API로 처리
25. 나의 기록 목록과 기록 상세는 최신 피그마 상태 전환 UI가 구현된 `dev` 버전을 `main`에 반영
26. 코스 만들기 4단계를 모바일 폭에 맞게 재배치하고, 선택 버튼·입력 필드·하단 CTA의 좁은 화면 오버플로를 제거
27. 추천 코스 모바일 화면에 상단 탭, 지도 바텀시트, 전체 일정 시트, 하단 탭바를 추가
28. 나의 기록 모바일 화면에 전용 헤더, 기본/데이터 보유 성향 카드 레이아웃, 하단 탭바를 추가
29. 저장한 코스 모바일 화면에 상태 탭과 상태별 카드, 다가오는 여행 카드, 전용 헤더·하단 탭바를 추가
30. 모바일 하단 메뉴를 공통 컴포넌트로 통합해 추천 코스와 나의 기록 계열 화면의 아이콘·활성 상태를 일치시킴
31. 모바일 하단 메뉴를 `MainShell` 전역 레이아웃으로 이동해 모든 화면에서 표시하고, 코스 만들기 CTA와 겹치지 않도록 하단 여백을 조정
32. 저장한 코스 데스크톱 화면에 `나의 기록 > 저장한 코스` 브레드크럼브를 복구
33. 저장한 코스 모바일 화면의 여백·카드 높이·목록 밀도를 피그마 기준으로 재조정해 상태 탭 줄바꿈을 제거
34. 신규 백엔드 API용 공통 same-origin 프록시와 플랫폼 API 클라이언트를 추가하고, 마이페이지 요약·최근 활동·알림 설정을 실제 API에 연결
35. 코스 생성의 `FAMILY`/`CALM` 동행 유형과 `startTime`, 저장 코스 상태·여행일·스탬프 진행률, 스탬프 필터·감정 필드를 최신 백엔드 명세에 맞춤
36. 저장 코스 다가오는 여행, 나의 기록 통계·성향, 완료 스탬프 테마·정렬을 실제 API 응답으로 연결하고 API 목록 전체를 공통 플랫폼 클라이언트로 제공
37. 실제 게스트 세션으로 프론트 프록시·인증 화면을 검증하고, API 성향 라벨을 화면 테마 라벨로 변환해 진행바 렌더링 오류를 방지
38. 장소가 없는 코스 생성 응답을 성공 코스로 전환하지 않고, 출발지·이동 방식 재선택 안내를 표시
39. 헤더의 로그인 사용자 이름 버튼을 제거하고, 로그인 상태에서는 로그아웃 버튼을 표시하도록 변경
40. 개발용 수동 로그인 버튼을 헤더에서 제거
41. 랜딩 감성 지도 스탬프를 로컬 완료/미완료 SVG 자산으로 교체해 외부 자산 로딩 실패를 제거
42. 랜딩 HOT 축제 카드 이미지를 로컬 단오제·속초·허브 PNG 자산으로 연결
43. 로그인 모달 닫기 아이콘을 외부 Figma 이미지 대신 로컬 Lucide 아이콘으로 교체
44. 로그인 모달 카카오 로그인 아이콘을 로컬 카카오톡 SVG 자산으로 교체
45. 코스 만들기 도착 날짜를 달력 UI와 폼 검증 모두에서 내일부터만 선택 가능하도록 제한
46. 코스 생성 성공 시 결과 데이터는 유지하고, 다음 코스 생성을 위한 선택 초안은 초기화
47. 추천 코스 일정 목록에서 지원하지 않는 `일정 추가하기` 동작을 제거
48. 추천 숙소 화면을 목업 대신 `/api/accommodations` 실제 응답으로 연결하고, 생성 코스 감성존 필터·지도 마커·로딩/오류/빈 상태를 추가
49. 추천 코스에서 실제 저장 코스 API로 저장·시작·완료를 전환하고, 재추천 확인·코스 종료 리뷰 모달과 스탬프 자격 판정/수령 CTA를 추가
50. 추천 코스 일정의 체크 배지는 시작·종료 지점이 아니라 실제 스탬프 수령 장소만 빨간색으로 표시하도록 수정
51. 자동차 출발 도시 선택 그리드의 반응형 열 수와 무관하게 모든 셀 경계선이 표시되도록 보더 규칙을 정리
52. 코스 만들기 진행바에서 완료한 단계를 로컬 `course-check` SVG로 표시해 외부 자산 의존을 제거
53. 나의 기록 데스크톱 보관함 카드의 우측 요약 영역을 실제 두 카드 폭에 맞춰 오버플로와 여백 불일치를 수정
54. 저장한 코스의 다가오는 여행 D-Day를 API의 여행일(`travelDate`) 기준으로 클라이언트에서 계산해 표시
55. 저장한 코스 데스크톱 목록에 대기중·진행중·완료 상태 필터와 상태별 카드 표시를 추가
56. 저장한 코스 데스크톱 목록의 필터 정렬과 카드 한 줄 밀도를 피그마 상태별 시안에 맞춰 조정
57. 저장 코스 목록 카드의 상세 화면 이동, 다가오는 여행 카드의 추천 코스 진입, 코스별 완료 스탬프 리뷰 모달 조회를 추가
58. 저장한 코스 목록의 아이콘을 상태 아이콘 대신 코스 만들기 여행 테마 SVG로 통일
59. 장소별 스탬프 수령 직후 리뷰 모달을 열고, 감정·한줄평을 `POST /api/records`의 `stampIds`와 함께 저장하도록 연동
60. 추천 숙소 지도에 `house-normal.svg` 마커, 선택 확대·비선택 색상·숙소 상세 사이드바를 추가
61. 코스 만들기 도착 날짜에 오늘을 허용하고, 오늘 선택 시 현재 시각 이후의 도착 시간만 선택·검증하도록 변경
62. 추천 코스 모바일 요약 영역의 중첩 버튼 구조를 제거해 hydration 오류를 수정
63. 추천 코스 지도에 장소 선택 전·후 마커 크기와 색상 상태, 선택 시 상세 사이드바 노출을 적용
64. 추천 숙소 지도 초기 전체 마커 범위 표시, 선택 숙소 중심 이동, 집 아이콘이 보이는 커스텀 마커로 보완
65. 추천 숙소 선택 확인·정보 입력 모달과 저장 코스 숙소 추가 API 연동을 구현
66. 추천 숙소 응답으로 중심 좌표가 갱신돼도 지도 인스턴스를 재생성하지 않고 초기 마커를 안정적으로 표시하도록 보완
67. 추천 숙소 화면 첫 진입 시 목록의 최상단 숙소 좌표를 지도 중심으로 표시하도록 조정
68. 추천 코스 일정 목록의 장소 선택 시 해당 좌표로 지도를 이동하도록 연결
69. 완료 스탬프 클릭 시 스탬프 ID에 연결된 기록의 감정·한줄 평가를 조회해 표시하도록 수정
70. 출발 역 드롭다운의 두 번째 옵션에만 적용되던 배경색 조건을 제거해 옵션 스타일을 통일
71. 오늘 도착 시간은 현재 시각을 15분 단위로 올림한 시각부터 선택·검증하도록 통일
72. 추천 코스가 저장된 상태에서는 데스크톱·모바일 북마크 아이콘을 빨간색 채움으로 표시
73. 코스 종료 리뷰를 저장 코스에 연결하고, 상세 화면에 한줄 평가·스탬프 기준 방문 수·장소별 방문 상태 아이콘을 표시
74. 나의 기록 화면의 하드코딩된 사용자명을 로그인 사용자 닉네임으로 교체
75. 심사자(게스트) 로그인 시 나의 기록 사용자명을 심사자로 표시
76. 추천 숙소 선택 시 `GET /api/accommodations/{contentId}` 상세 조회를 호출하고 숙소 상세 패널에 이용 정보를 표시
77. 랜딩 HOT 축제 영역을 `GET /api/festivals?weekendOnly=true` 응답 기반 캐러셀로 교체
78. 로그인 진입을 웹의 게스트 로그인 모달 전환과 모바일 전용 `/login` 선택 화면으로 분리하고, 게스트 버튼 클릭 시 `/login/guest` 입력 페이지로 이동하도록 구성
79. 모바일 로그인 페이지의 `useSearchParams`를 Suspense 경계로 감싸 프로덕션 프리렌더링 오류를 수정
80. 카카오 로그인 후 SDK 프로필 닉네임을 반영하고 자동생성 카카오 닉네임은 화면에 노출하지 않도록 보완
81. 피그마 모바일 랜딩(`1445:2166`) 기준으로 전용 헤더, 가로 코스·방법 카드, 스탬프 영역의 크기와 여백을 재정렬
82. 루트 viewport를 기기 폭 기준으로 설정하고, 모바일 랜딩 섹션 제목의 좁은 폭 줄바꿈을 보완
83. 모바일 랜딩의 콘텐츠 좌우 여백을 32px로 통일하고, 코스 만드는 방법 카드를 138px 높이로 조정
84. 로그아웃 시 인증 가드보다 먼저 랜딩으로 이동시키고 로그인 모달이 다시 열리지 않도록 처리
85. 추천 코스 결과에서 브라우저 뒤로가기 시 새 코스 생성 확인 모달을 노출하고, 확인 후 1단계로 이동하도록 처리
86. 랜딩 축제 캐러셀 버튼을 카드 단위 스냅 이동으로 변경해 카드가 중간에서 잘리지 않도록 보완
87. 추천 코스 SPOT ID 기반 공개 코스 후기 API와 지도·사이드바 후기 탭을 추가
88. 모바일 랜딩의 가로 카드 영역을 카드 단위 스냅으로 통일해 스크롤 중간에서 카드가 잘려 보이지 않도록 보완
89. 나의 기록 여행 성향을 `GET /api/records/traits`의 대표 유형·존별 비율로 표시하고, 임시 미리보기 전환을 제거
90. 코스 종료·장소 리뷰 저장 전 `travelDate`를 `YYYY-MM-DD`로 정규화해 백엔드 날짜 검증 오류를 방지

## 결정 사항

- 제품 범위는 강원도 하루 여행 코스 생성과 기록 흐름 중심으로 고정
- GNB에서 서비스 소개는 제외
- 여행 타입은 단일 선택
- 코스 결과의 공유 기능은 제외하고 기록 카드 상세에서만 유지
- 숙소는 추천 선택/직접 입력까지만 1차 범위에 포함
- 추천 코스 결과 화면의 지도, 추천 이유, 숙소, 동행 감정 섹션은 전부 Mock UI로 먼저 검증
- 코스 상세 화면은 `/course/saved` 라우트에서 추천 코스의 읽기 전용 상세 Mock으로 우선 제공
- 추천 코스, 코스 상세, 나의 기록은 `src/components/mock-pages/MockPageShared.tsx` 공통 레이어를 우선 재사용
- 코스 생성 초안은 `localStorage`가 아니라 `sessionStorage`에 저장
- 전역 상태는 이미 설치된 `zustand`를 그대로 사용
- 실제 생성 호출은 4단계에서만 `POST /api/courses/generate`
- 로그인 판별은 서비스 JWT `accessToken` 존재 여부를 기준으로 두고, 사용자 정보는 `/api/auth/me`로 재동기화
- 로그인 상태는 새로고침 후에도 유지되어야 하므로 `sessionStorage`가 아니라 `localStorage`에 저장
- 인증 요청은 브라우저에서 운영 API를 직접 호출하지 않고 same-origin Next proxy route를 경유
- `dev`의 공통 API client 패턴은 유지하되, 현재 브랜치의 same-origin `/api/*` 프록시 구조에 맞춰 상대 경로 기반으로 사용
- 추천 코스 지도는 API 응답만 표시하며, 테스트 추천 코스 데이터로 대체하지 않는다.

## 막힘

- 실제 출발지 좌표 전체 매핑은 아직 없음
- 실제 OAuth, 지도 SDK, 위치 인증 서버 정책은 미확정
- 실제 저장/재추천/권한 가드 로직은 아직 미구현
- Google 로그인 지원 범위와 실제 운영 도메인의 Kakao JavaScript 키 등록 상태는 추후 확정 필요
- 운영 백엔드 auth 경로를 `/api/auth/*` 하나로 고정할지 `/auth/*`로 고정할지 최종 합의 필요

## 다음 행동

1. 4단계 UI를 피그마 기준으로 세부 구현
2. 출발지별 좌표 사전 확장 또는 역/터미널 조회 API 연동
3. `POST /api/courses/generate` 로딩/에러/재시도 UX 보강
4. 결과 페이지를 실제 응답 구조 기준으로 상세 매핑
5. Kakao 운영 도메인 등록 상태 확인 및 Google 로그인 추가 여부 확정
6. 맥미니 `actions-runner`에서 `./svc.sh install && ./svc.sh start` 실행 후 서비스 상태 확인
7. 랜딩페이지 모바일/실서버 배포 결과를 피그마와 재대조
8. 기록 카드 작성·상세·삭제와 여행 메이트 API를 화면 사용자 흐름에 연결
9. 추천 코스 결과 화면의 숙소/재추천/같은 코스 감정 섹션을 실제 API 응답과 연결
10. 코스 상세 화면의 장소별 링크를 실제 장소 상세 라우트와 연결
11. 백엔드 auth endpoint 확정 후 Next proxy의 fallback 경로 제거

## TODO

### 2026-08-12 / 타입: TODO

- 코드 교체 시점 및 조건: Google 로그인 추가 또는 Auth API 경로 확정 시
- 개발 내용
  - `src/api/auth/kakao-login.ts`와 `src/api/auth/guest-login.ts`의 fallback 경로를 백엔드 확정 경로 하나로 정리
  - Kakao JavaScript 키와 도메인 등록 상태를 실제 운영 환경 기준으로 검증
  - Google 로그인 스펙 확정 시 `AuthProvider`와 로그인 모달 CTA 확장

## 환경 분리

### Dev

- 목업 데이터 중심
- 심사자 모드 기본 활성화 가능
- 로컬 `.env.local` 사용

### Production

- 실제 OAuth와 API 사용
- 서버 위치 검증 필수
- 심사자 데모는 별도 플래그로 제한
