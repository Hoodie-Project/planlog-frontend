# AGENTS

- `docs/prd.md`, `docs/architecture.md`, `docs/progress.md` 순서로 읽고 갱신한다.
- 제품 결정은 먼저 `prd.md`, 구현 구조는 `architecture.md`, 실행 상태와 다음 행동은 `progress.md`에 반영한다.
- 비밀값은 `.env`에만 저장하고 Git에는 커밋하지 않는다.
- 개발 환경과 운영 환경의 설정, 데이터, 배포 절차를 분리한다.
- 범위 확장은 `prd.md`의 우선순위와 검증 기준을 먼저 갱신한 뒤 진행한다.
- 문서와 코드 간 불일치가 생기면 문서부터 수정하지 말고 실제 의도를 확인한다.
- 사용자 요청 범위를 벗어난 리팩터링과 디자인 과장은 금지한다.

